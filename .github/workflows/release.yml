name: Release

on:
  push:
    branches: [main]
    paths:
      - 'Sources/**'
      - 'Resources/**'
      - 'Package.swift'
      - 'Package.resolved'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Calculate version
        id: calc-version
        shell: bash
        run: |
          git fetch --tags || true
          # Find latest semver tag, default to v0.0.0
          LATEST=$(git tag -l 'v[0-9]*.[0-9]*.[0-9]*' --sort=-v:refname | head -n1)
          LATEST=${LATEST:-v0.0.0}
          # Bump patch version
          MAJOR=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f1)
          MINOR=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f2)
          PATCH=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f3)
          TAG="v${MAJOR}.${MINOR}.$((PATCH + 1))"
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "prev=$LATEST" >> $GITHUB_OUTPUT
          if git show-ref --tags --verify --quiet "refs/tags/$TAG"; then
            echo "skip=yes" >> $GITHUB_OUTPUT
          else
            echo "skip=no" >> $GITHUB_OUTPUT
          fi

      - name: Build
        if: steps.calc-version.outputs.skip == 'no'
        run: swift build -c release

      - name: Package
        if: steps.calc-version.outputs.skip == 'no'
        env:
          VER: ${{ steps.calc-version.outputs.tag }}
        run: |
          ./scripts/install.sh --no-install --version "$VER"

      - name: Notes
        if: steps.calc-version.outputs.skip == 'no'
        run: |
          PREV="${{ steps.calc-version.outputs.prev }}"
          
          # Always start with header
          echo "## HueBar" > notes.txt
          echo "" >> notes.txt
          echo "Native macOS menubar app for controlling Philips Hue lights" >> notes.txt
          echo "" >> notes.txt
          
          # Generate changelog
          if [ -n "$PREV" ]; then
            echo "### What's Changed" >> notes.txt
            echo "" >> notes.txt
            git log --pretty=format:"- %s" ${PREV}..HEAD >> notes.txt
            echo "" >> notes.txt
          else
            echo "### Features" >> notes.txt
            echo "" >> notes.txt
            echo "- 💡 Room and zone control with on/off toggles" >> notes.txt
            echo "- 🎚️ Brightness and color adjustment" >> notes.txt
            echo "- 🎨 Scene activation and browsing" >> notes.txt
            echo "- 💡 Individual light control" >> notes.txt
            echo "- ⌨️ Global keyboard shortcuts" >> notes.txt
            echo "- 😴 Sleep/wake automation" >> notes.txt
            echo "- 🔄 Real-time updates via SSE" >> notes.txt
            echo "" >> notes.txt
          fi
          
          echo "## Installation" >> notes.txt
          echo "" >> notes.txt
          echo "1. Download **HueBar.zip** from the Assets section below" >> notes.txt
          echo "2. Unzip and move **HueBar.app** to \`/Applications\`" >> notes.txt
          echo "3. On first launch, **right-click → Open** (the app is ad-hoc signed, so Gatekeeper will ask for confirmation once)" >> notes.txt
          echo "4. Connect to your Hue Bridge when prompted" >> notes.txt
          echo "" >> notes.txt
          echo "**Requirements:** macOS 15.0 (Sequoia) or later" >> notes.txt

      - name: Release
        if: steps.calc-version.outputs.skip == 'no'
        uses: softprops/action-gh-release@c062e08bd532815e2082a85e87e3ef29c3e6d191
        with:
          tag_name: ${{ steps.calc-version.outputs.tag }}
          name: ${{ steps.calc-version.outputs.tag }}
          body_path: notes.txt
          files: HueBar.zip
